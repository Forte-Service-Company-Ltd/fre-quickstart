{
  "Policy": "Test Policy",
  "Description": "This policy is designed to test the transfer function with a value greater than 10000.",
  "PolicyType": "open",
  "CallingFunctions": [
    {
      "name": "transfer(address to, uint256 value)",
      "functionSignature": "transfer(address to, uint256 value)",
      "encodedValues": "address to, uint256 value, string tag"
    }
  ],
  "ForeignCalls": [],
  "Trackers": [
    {
      "name": "trackerOne",
      "type": "uint256",
      "initialValue": "0"
    },
    {
      "name": "washTradingTracker",
      "type": "uint256",
      "initialValue": "0"
    }
  ],
  "MappedTrackers": [],
  "Rules": [
    {
      "Name": "tradingVolume",
      "Description": "Revert if volume has gone above 5000",
      "condition": "TR:trackerOne < 5000",
      "positiveEffects": ["emit \"TransferSuccessful\" ", "TRU:trackerOne += value"],
      "negativeEffects": ["revert(\"Transfer Failed\")"],
      "callingFunction": "transfer(address to, uint256 value)"
    },
    {
      "Name": "washTrading",
      "Description": "Revert if a transfer happens in less than 30 seconds",
      "condition": "GV:BLOCK_TIMESTAMP - TR:washTradingTracker > 30",
      "positiveEffects": ["emit \"TransferSuccessful\" ", "TRU:washTradingTracker = GV:BLOCK_TIMESTAMP"],
      "negativeEffects": ["revert(\"Transfer Failed\")"],
      "callingFunction": "transfer(address to, uint256 value)"
    }
  ]
}
