{
  "Policy": "Test Policy",
  "Description": "This policy is designed to test the transfer function with a value greater than 10000.",
  "PolicyType": "open",
  "CallingFunctions": [
    {
      "name": "transfer(address to, uint256 value, bytes data)",
      "functionSignature": "transfer(address to, uint256 value, bytes data)",
      "encodedValues": "address to, uint256 value, bytes data"
    }
  ],
  "ForeignCalls": [],
  "Trackers": [
    {
      "name": "trackerOne",
      "type": "bytes",
      "initialValue": "0xdeadbeef"
    }
  ],
  "MappedTrackers": [],
  "Rules": [
    {
      "Name": "TransferRule",
      "Description": "Rule to check if the transfer value is greater than 10000.",
      "condition": "TR:trackerOne == 0xdeadbeef",
      "positiveEffects": ["TR:trackerOne = 0xdeadbeefdeadbeef"],
      "negativeEffects": ["revert(\"Transfer Failed\")"],
      "callingFunction": "transfer(address to, uint256 value, bytes data)"
    }
  ]
}
